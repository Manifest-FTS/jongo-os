[Unit]
Description=Run Coolify WordPress Auto-Ops Sync Hourly

[Timer]
# Run at the top of every hour
OnCalendar=hourly
# Or use this to run every 15 minutes: OnCalendar=*:0/15
Persistent=true
RandomizedDelaySec=60

[Install]
WantedBy=timers.target
